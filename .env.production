# En producción con Cloudflare Pages Functions, no se necesita esta variable
# Las funciones se sirven automáticamente en /api/*
# 
# Si usas Worker separado (no recomendado), descomenta y configura:
# VITE_API_URL=https://your-worker-url.workers.dev
